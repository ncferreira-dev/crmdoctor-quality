DATABASE_URL="postgresql://...-pooler.../neondb?sslmode=require"
DIRECT_URL="postgresql://...(sem -pooler).../neondb?sslmode=require"
JWT_SECRET="troque-isto"
FRONTEND_URL="http://localhost:3000"
PORT=3001
TZ="America/Sao_Paulo"
SEED_ADMIN_PASSWORD="troque-isto-tambem"

# E-mail transacional (Resend). Sem RESEND_API_KEY o motor sobe DESLIGADO e
# avisa no log: a API funciona e nenhum aviso de compliance sai do sistema.
# Como obter a chave: CHAVES-PENDENTES.md, na raiz do repositorio.
# EMAIL_REMETENTE so vale depois do dominio verificado no Resend; sem ele o
# envio usa o dominio de teste, que so entrega para o dono da conta.
RESEND_API_KEY="re_..."
EMAIL_REMETENTE="CRM Doctor Quality <avisos@doctorquality.com.br>"
